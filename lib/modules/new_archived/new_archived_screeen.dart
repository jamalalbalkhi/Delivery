import 'package:flutter/cupertino.dart';

class NewArchivedScreeen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('New Archived ',style: TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 30,
        ),),
    );
  }
}
