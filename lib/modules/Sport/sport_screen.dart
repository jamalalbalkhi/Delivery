import 'package:flutter/cupertino.dart';

class SportScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sport Screen",style: TextStyle(
        fontSize: 30,
        fontWeight: FontWeight.bold
      ),
      ),
    );
  }
}
